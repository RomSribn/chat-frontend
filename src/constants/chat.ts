export const CHAT_CONSTANTS = {
  // Scroll threshold for loading previous messages (in pixels)
  SCROLL_THRESHOLD: 50,

  // Default number of messages per page
  DEFAULT_LIMIT: 20,

  // Automatic error hiding time (in milliseconds)
  ERROR_AUTO_DISMISS: 5000,
};
